Question for the day:
You will be given a number(decimal) and you will need to return it as a string in Expanded Form. For example:

expandedForm(1.24); // should return '1 + 2/10 + 4/100'
expandedForm(7.304); // should return '7 + 3/10 + 4/1000'
expandedForm(0.04); // should return '4/100'

1-convert number to string
2-split whole from decimal
3-proccess whole number using previous answer
4-process decimal with new solution